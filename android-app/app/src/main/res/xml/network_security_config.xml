<?xml version="1.0" encoding="utf-8"?>
<!-- The café server runs on the local Wi-Fi over plain HTTP (no certificate), so cleartext must be allowed. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
